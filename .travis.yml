language: cpp
compiler:
  - clang
  - gcc
os:
  - osx
#  - linux

osx_image: xcode7.3

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update && brew install scons; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" && "$CC" == "gcc" ]]; then export CC=gcc-5; export CXX=g++-5; fi
  - $CXX -v

script:
  - scons -C deps
  - mkdir build
  - cd build
  - cmake .. && make && ./wtest_suite --durations yes --warn NoAssertions
